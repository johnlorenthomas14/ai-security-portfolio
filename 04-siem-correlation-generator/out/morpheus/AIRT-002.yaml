# rule_id: AIRT-002
# name: System Prompt Leakage Confirmed by Red-Team Probe
# severity: high
# owasp: LLM07
# atlas: AML.T0051
# airmf: MEASURE 2.7
# description: Fires when a Project 2 red-team probe in the LLM07 (System Prompt Leakage) category lands a fail verdict. The probe tests whether the target LLM application's system prompt can be extracted via direct request, repeat-verbatim phrasing, or roleplay-extraction — and the fail verdict confirms it can. System-prompt leakage is foundational for further attacks (override, code extraction, refusal bypass) so this is treated as a high-priority finding.
#
# Generated by Project 4 SIEM Correlation Generator. Drop into a
# Morpheus deployment under config/pipelines/. The stage list is
# canonical — translate each entry into pipeline.add_stage(...)
# calls in your runner script. Filter conditions are cuDF-
# compatible boolean expressions evaluated on the GPU dataframe.

pipeline:
  name: ai-security-airt-002
  feature_length: 256
  num_threads: 4
  pipeline_batch_size: 1024
  edge_buffer_size: 128

  source:
    type: from-kafka
    bootstrap_servers: ${KAFKA_BOOTSTRAP_SERVERS}
    input_topic: aisec-events
    group_id: morpheus-airt-002

  stages:
    - type: deserialize

    - type: monitor
      description: "Source — incoming aisec events"

    - type: filter
      filter_source: PAYLOAD
      condition: 'cim_eventtype == "ai_red_team_finding" and owasp_category == "LLM07"'

    - type: monitor
      description: "Matches — AIRT-002 condition fired"

    - type: aggregate
      group_by: [target_name]
      threshold_count: 1
      window_seconds: 86400

    - type: add-class
      class_name: notable_event
      severity: high
      drilldown_field: probe_id
      rule_id: AIRT-002

    - type: serialize

  sink:
    type: to-kafka
    bootstrap_servers: ${KAFKA_BOOTSTRAP_SERVERS}
    output_topic: aisec-notables

tags:
  rule_id: AIRT-002
  owasp_llm: LLM07
  mitre_atlas: [AML.T0051]
  airmf: MEASURE 2.7
  source_index: aisec
  sourcetype: "aisec:red_team"
