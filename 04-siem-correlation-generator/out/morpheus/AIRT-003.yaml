# rule_id: AIRT-003
# name: Jailbreak Persona Adoption Confirmed
# severity: high
# owasp: LLM09
# atlas: AML.T0054
# airmf: MEASURE 2.6
# description: Fires on a Project 2 red-team probe in the LLM09 (Misinformation / Jailbreak) category that landed a fail verdict. Confirms the target adopted a named persona override (DAN, AIM, developer-mode, etc.) — i.e., the model's RLHF-trained refusal posture was successfully bypassed. Once a persona override succeeds, the model is effectively ungoverned for the rest of that conversation.
#
# Generated by Project 4 SIEM Correlation Generator. Drop into a
# Morpheus deployment under config/pipelines/. The stage list is
# canonical — translate each entry into pipeline.add_stage(...)
# calls in your runner script. Filter conditions are cuDF-
# compatible boolean expressions evaluated on the GPU dataframe.

pipeline:
  name: ai-security-airt-003
  feature_length: 256
  num_threads: 4
  pipeline_batch_size: 1024
  edge_buffer_size: 128

  source:
    type: from-kafka
    bootstrap_servers: ${KAFKA_BOOTSTRAP_SERVERS}
    input_topic: aisec-events
    group_id: morpheus-airt-003

  stages:
    - type: deserialize

    - type: monitor
      description: "Source — incoming aisec events"

    - type: filter
      filter_source: PAYLOAD
      condition: 'cim_eventtype == "ai_red_team_finding" and owasp_category == "LLM09"'

    - type: monitor
      description: "Matches — AIRT-003 condition fired"

    - type: aggregate
      group_by: [target_name]
      threshold_count: 1
      window_seconds: 86400

    - type: add-class
      class_name: notable_event
      severity: high
      drilldown_field: probe_id
      rule_id: AIRT-003

    - type: serialize

  sink:
    type: to-kafka
    bootstrap_servers: ${KAFKA_BOOTSTRAP_SERVERS}
    output_topic: aisec-notables

tags:
  rule_id: AIRT-003
  owasp_llm: LLM09
  mitre_atlas: [AML.T0054]
  airmf: MEASURE 2.6
  source_index: aisec
  sourcetype: "aisec:red_team"
