# Auto-generated from rules/AIPI-002.yaml — do not edit by hand
# OWASP: LLM01  ATLAS: AML.T0051, AML.T0054  AI RMF: MEASURE 2.7
[Suspicious Prompt Injection Burst from Single User]
description = Catches a burst of suspicious-verdict events from one user in a short window. Single suspicious events are common (clumsy phrasing, edge-case inputs) but a burst from one user inside an hour is the access-pattern signature of someone iterating on injection payloads to find one that works. Threshold tunable per environment.
search = index=aisec sourcetype="aisec:prompt_injection" cim_eventtype="ai_prompt_injection" verdict="suspicious" | stats count by user | where count >= 5
dispatch.earliest_time = -1h
dispatch.latest_time = now
enableSched = 1
cron_schedule = */5 * * * *
alert.track = 1
alert.severity = 3
action.notable = 1
action.notable.param.severity = medium
action.notable.param.rule_title = Suspicious Prompt Injection Burst from Single User
action.notable.param.rule_description = Catches a burst of suspicious-verdict events from one user in a short window. Single suspicious events are common (clumsy phrasing, edge-case inputs) but a burst from one user inside an hour is the access-pattern signature of someone iterating on injection payloads to find one that works. Threshold tunable per environment.
action.notable.param.drilldown_name = Pivot on src_input_excerpt
action.notable.param.drilldown_search = index=aisec sourcetype="aisec:prompt_injection" src_input_excerpt="$result.src_input_excerpt$"
