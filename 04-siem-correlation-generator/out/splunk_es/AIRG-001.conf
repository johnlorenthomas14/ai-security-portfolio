# Auto-generated from rules/AIRG-001.yaml — do not edit by hand
# OWASP: LLM02  ATLAS: AML.T0024  AI RMF: MEASURE 2.10
[Credentials Detected in RAG Corpus Document]
description = Fires when the Project 3 RAG auditor finds a credential leak in a corpus document — AWS keys, GitHub tokens, Anthropic / OpenAI keys, PEM-block private keys, or .env-style credential variables. Once a document is in the corpus, retrieval can surface its contents to any authorized user — meaning a credential in the corpus is effectively shared with the entire authorized-user population.
search = index=aisec sourcetype="aisec:rag_audit" cim_eventtype="ai_rag_finding" category="secret_leak" | stats count by document_path | where count >= 1
dispatch.earliest_time = -24h
dispatch.latest_time = now
enableSched = 1
cron_schedule = */5 * * * *
alert.track = 1
alert.severity = 6
action.notable = 1
action.notable.param.severity = critical
action.notable.param.rule_title = Credentials Detected in RAG Corpus Document
action.notable.param.rule_description = Fires when the Project 3 RAG auditor finds a credential leak in a corpus document — AWS keys, GitHub tokens, Anthropic / OpenAI keys, PEM-block private keys, or .env-style credential variables. Once a document is in the corpus, retrieval can surface its contents to any authorized user — meaning a credential in the corpus is effectively shared with the entire authorized-user population.
action.notable.param.drilldown_name = Pivot on document_path
action.notable.param.drilldown_search = index=aisec sourcetype="aisec:rag_audit" document_path="$result.document_path$"
