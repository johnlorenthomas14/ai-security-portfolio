# Auto-generated from rules/AIRT-001.yaml — do not edit by hand
# OWASP: LLM01  ATLAS: AML.T0051, AML.T0054  AI RMF: MEASURE 2.7
[Red-Team Probe Identified Capitulation in Production Target]
description = Fires on any fail-verdict event from a Project 2 red-team scan. A fail verdict means the framework confirmed the target capitulated to a probe — i.e., a reproducible vulnerability against a deployed LLM application. Critical findings indicate confirmed control gaps that require immediate Manage-function action: system-prompt update, input-layer detector deployment, or output-layer guardrail.
search = index=aisec sourcetype="aisec:red_team" cim_eventtype="ai_red_team_finding" verdict="fail" | stats count by target_name, owasp_category | where count >= 1
dispatch.earliest_time = -1h
dispatch.latest_time = now
enableSched = 1
cron_schedule = */5 * * * *
alert.track = 1
alert.severity = 5
action.notable = 1
action.notable.param.severity = high
action.notable.param.rule_title = Red-Team Probe Identified Capitulation in Production Target
action.notable.param.rule_description = Fires on any fail-verdict event from a Project 2 red-team scan. A fail verdict means the framework confirmed the target capitulated to a probe — i.e., a reproducible vulnerability against a deployed LLM application. Critical findings indicate confirmed control gaps that require immediate Manage-function action: system-prompt update, input-layer detector deployment, or output-layer guardrail.
action.notable.param.drilldown_name = Pivot on probe_id
action.notable.param.drilldown_search = index=aisec sourcetype="aisec:red_team" probe_id="$result.probe_id$"
