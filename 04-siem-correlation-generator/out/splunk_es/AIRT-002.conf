# Auto-generated from rules/AIRT-002.yaml — do not edit by hand
# OWASP: LLM07  ATLAS: AML.T0051  AI RMF: MEASURE 2.7
[System Prompt Leakage Confirmed by Red-Team Probe]
description = Fires when a Project 2 red-team probe in the LLM07 (System Prompt Leakage) category lands a fail verdict. The probe tests whether the target LLM application's system prompt can be extracted via direct request, repeat-verbatim phrasing, or roleplay-extraction — and the fail verdict confirms it can. System-prompt leakage is foundational for further attacks (override, code extraction, refusal bypass) so this is treated as a high-priority finding.
search = index=aisec sourcetype="aisec:red_team" cim_eventtype="ai_red_team_finding" owasp_category="LLM07" | stats count by target_name | where count >= 1
dispatch.earliest_time = -24h
dispatch.latest_time = now
enableSched = 1
cron_schedule = */5 * * * *
alert.track = 1
alert.severity = 5
action.notable = 1
action.notable.param.severity = high
action.notable.param.rule_title = System Prompt Leakage Confirmed by Red-Team Probe
action.notable.param.rule_description = Fires when a Project 2 red-team probe in the LLM07 (System Prompt Leakage) category lands a fail verdict. The probe tests whether the target LLM application's system prompt can be extracted via direct request, repeat-verbatim phrasing, or roleplay-extraction — and the fail verdict confirms it can. System-prompt leakage is foundational for further attacks (override, code extraction, refusal bypass) so this is treated as a high-priority finding.
action.notable.param.drilldown_name = Pivot on probe_id
action.notable.param.drilldown_search = index=aisec sourcetype="aisec:red_team" probe_id="$result.probe_id$"
