# Auto-generated from rules/AIRT-003.yaml — do not edit by hand
# OWASP: LLM09  ATLAS: AML.T0054  AI RMF: MEASURE 2.6
[Jailbreak Persona Adoption Confirmed]
description = Fires on a Project 2 red-team probe in the LLM09 (Misinformation / Jailbreak) category that landed a fail verdict. Confirms the target adopted a named persona override (DAN, AIM, developer-mode, etc.) — i.e., the model's RLHF-trained refusal posture was successfully bypassed. Once a persona override succeeds, the model is effectively ungoverned for the rest of that conversation.
search = index=aisec sourcetype="aisec:red_team" cim_eventtype="ai_red_team_finding" owasp_category="LLM09" | stats count by target_name | where count >= 1
dispatch.earliest_time = -24h
dispatch.latest_time = now
enableSched = 1
cron_schedule = */5 * * * *
alert.track = 1
alert.severity = 5
action.notable = 1
action.notable.param.severity = high
action.notable.param.rule_title = Jailbreak Persona Adoption Confirmed
action.notable.param.rule_description = Fires on a Project 2 red-team probe in the LLM09 (Misinformation / Jailbreak) category that landed a fail verdict. Confirms the target adopted a named persona override (DAN, AIM, developer-mode, etc.) — i.e., the model's RLHF-trained refusal posture was successfully bypassed. Once a persona override succeeds, the model is effectively ungoverned for the rest of that conversation.
action.notable.param.drilldown_name = Pivot on probe_id
action.notable.param.drilldown_search = index=aisec sourcetype="aisec:red_team" probe_id="$result.probe_id$"
